# Gradle
.gradle/
build/
**/build/
local.properties

# Signing key — never committed
*.jks

# Proprietary ECarX SDK jars — plaintext stays local; committed only as
# encrypted *.enc blobs (decrypted in CI via the LIBS_ENC_PASSWORD secret).
app/libs/*.jar

# IDE
.idea/
*.iml
.vscode/

# Android Studio captures
captures/

# OS junk
.DS_Store
Thumbs.db
